<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome!</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      /* Özel stil eklemeleri */
      body {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
        background-color: #f8f9fa;
      }

      a {
        color: inherit; /* Linklerin genel renk ayarı */
        text-decoration: none; /* Link çizgisini kaldırma */
      }

      .custom-box {
        padding: 80px;
        margin: 10px;
        text-align: center;
        border-radius: 10px;
      }

      .user-details-box {
        background-color: #6c757d; /* Koyu gri */
        color: #fff;
      }

      .log-in-box {
        background-color: #6c757d; /* Koyu gri */
        color: #fff;
      }

      .button-box {
        margin-top: 20px;
      }

      .btn {
        background-color: #adb5bd; /* Gri */
        color: #000; /* Siyah */
        border: none; /* Kenarlık yok */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="mb-5">Welcome!</h1>
      <div class="row">
        <% if (isLogged) { %>
        <div class="row">
          <div class="col-md-12">
            <div class="alert alert-success" role="alert">
              You successfully logged in
            </div>
          </div>
        </div>
        <% } %>
      </div>
      <div class="row">
        <div class="col-md-6">
          <!-- User Details Box -->
          <div class="custom-box user-details-box">
            <h3>User Details</h3>
            <div class="button-box">
              <button class="btn">
                <a href="http://localhost:8080/users">User Details</a>
              </button>
            </div>
          </div>
        </div>

        <div class="col-md-6">
          <!-- Log In Box -->
          <div class="custom-box log-in-box">
            <h3>Log In</h3>
            <div class="button-box">
              <button class="btn">
                <a href="http://localhost:8080/form">Log In</a>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS ve Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
